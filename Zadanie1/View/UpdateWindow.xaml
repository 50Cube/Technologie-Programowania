<Window x:Class="View.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        xmlns:local="clr-namespace:View"
        mc:Ignorable="d"
        Title="UpdateWindow" Height="450" Width="500" ResizeMode="NoResize">

    <Window.DataContext>
        <vm:ViewModelClass />
    </Window.DataContext>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        
        <Label Content="Enter ID of chosen product" Grid.Row="0" Grid.Column="0"/>
        <TextBox Text="{Binding ID}" Grid.Row="0" Grid.Column="1"/>

        <Label Content="New name" Grid.Row="2" Grid.Column="0"/>
        <TextBox Text="{Binding Name}" Grid.Row="2" Grid.Column="1"/>

        <Button Content="Save changes" Command="{Binding UpdateProductCommand}" Grid.Row="20" Width="120" HorizontalAlignment="Center"/>
    </Grid>
</Window>
